# =============================================================================
# Slurpy Environment Variables - TEMPLATE
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env.local (for development) or .env.production
# 2. Fill in all the values
# 3. NEVER commit .env files to Git!
#
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT
# -----------------------------------------------------------------------------
NODE_ENV=development  # development | production
ENVIRONMENT=development  # Used by backend

# -----------------------------------------------------------------------------
# FRONTEND - Next.js
# -----------------------------------------------------------------------------

# Clerk Authentication
# Get these from: https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx  # Use pk_live_xxx in production
CLERK_SECRET_KEY=sk_test_xxx  # Use sk_live_xxx in production
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat

# Supabase
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx

# Stripe (if using payments)
# Get these from: https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx  # Use pk_live_xxx in production
STRIPE_SECRET_KEY=sk_test_xxx  # Use sk_live_xxx in production
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Stripe Price IDs (from Stripe dashboard > Products)
NEXT_PUBLIC_STRIPE_PRICE_ID_BASIC=price_xxx
NEXT_PUBLIC_STRIPE_PRICE_ID_PRO=price_xxx
NEXT_PUBLIC_STRIPE_PRICE_ID_ENTERPRISE=price_xxx

# Sentry (Error Tracking)
# Get from: https://sentry.io
SENTRY_DSN=https://xxx@sentry.io/xxx
SENTRY_AUTH_TOKEN=xxx  # For uploading source maps
NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000  # Use https://api.your-domain.com in production

# -----------------------------------------------------------------------------
# BACKEND - FastAPI
# -----------------------------------------------------------------------------

# Supabase (Service Role - Has admin privileges!)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJxxx  # Service role key, NOT anon key!
SUPABASE_ANON_KEY=eyJxxx  # Anon key for client-side operations

# OpenAI
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxx

# Qdrant Vector Database
QDRANT_URL=http://localhost:6333  # Or Qdrant Cloud URL
QDRANT_API_KEY=xxx  # Only if using Qdrant Cloud

# CORS Settings (Production only)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Sentry
SENTRY_DSN=https://xxx@sentry.io/xxx
SENTRY_ENVIRONMENT=development  # Change to 'production' in prod

# Rate Limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=60

# -----------------------------------------------------------------------------
# MCP SERVER
# -----------------------------------------------------------------------------

OPENAI_API_KEY=sk-xxx
QDRANT_URL=http://localhost:6333
SENTRY_DSN=https://xxx@sentry.io/xxx

# -----------------------------------------------------------------------------
# DATABASE (If not using Supabase)
# -----------------------------------------------------------------------------

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/slurpy

# -----------------------------------------------------------------------------
# OPTIONAL - Advanced Features
# -----------------------------------------------------------------------------

# Redis (for caching and rate limiting)
REDIS_URL=redis://localhost:6379

# SendGrid (for transactional emails)
SENDGRID_API_KEY=SG.xxx

# AWS S3 (for file storage)
AWS_ACCESS_KEY_ID=xxx
AWS_SECRET_ACCESS_KEY=xxx
AWS_S3_BUCKET=slurpy-uploads
AWS_REGION=us-east-1

# Posthog (Analytics)
NEXT_PUBLIC_POSTHOG_KEY=phc_xxx
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Feature Flags
ENABLE_BETA_FEATURES=false
ENABLE_NEW_CHAT_UI=false

# Monitoring
LOG_LEVEL=info  # debug | info | warning | error | critical

# -----------------------------------------------------------------------------
# FLY.IO DEPLOYMENT
# -----------------------------------------------------------------------------

# Set these as Fly.io secrets, not in .env:
# fly secrets set CLERK_SECRET_KEY=sk_live_xxx --app slurpy-frontend
# fly secrets set SUPABASE_SERVICE_ROLE_KEY=eyJxxx --app slurpy-backend
# fly secrets set OPENAI_API_KEY=sk-xxx --app slurpy-mcp

# App Names (update with your actual Fly.io app names)
FLY_APP_FRONTEND=slurpy-frontend
FLY_APP_BACKEND=slurpy-backend
FLY_APP_MCP=slurpy-mcp

# -----------------------------------------------------------------------------
# SECURITY NOTES
# -----------------------------------------------------------------------------
# 
# 1. NEVER commit .env files to Git
# 2. Use different keys for development and production
# 3. Rotate API keys every 90 days
# 4. Use Fly.io secrets for production (not .env files)
# 5. Keep service role keys SECRET (they have admin access)
# 6. Use read-only keys where possible
#
# =============================================================================
